# Copy this file to backend/.env and restart the FastAPI server.
# backend/.env is ignored by Git. Never place the key in docs/ or frontend code.
AI_PROVIDER_ENABLED=false
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
