# Love 21 static site

Published via GitHub Pages from this `docs/` folder (`main` → `/docs`).

Local + API: apply Supabase migrations, then run `cd backend && uvicorn app.main:app --reload --port 8000` and open http://127.0.0.1:8000
